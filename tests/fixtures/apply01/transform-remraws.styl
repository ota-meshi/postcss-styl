/* based from postcss-parser-tests/cases/apply.css */
:root {
    --zero-size: {
    width: 0
    height: 0
  };
    --small-icon: {
    width: 16px
    height: 16px
  }
}
.toolbar {
    @apply --zero-size
}
.toolbar> .title {
    @apply --small-icon
}

